<h1><%= @forum.name %></h1>

<% for topic in @forum.topics %>
  <h3><%= link_to topic.name, forum_topic_path(topic.id) %></h3>
    <p class="details"><%= topic.posts.count %> Posts</p>
    <p class="details">Last Post <%= time_ago_in_words(topic.posts.last) %></p>
    <p class="description"><%= topic.description %></p>
<% end %>
