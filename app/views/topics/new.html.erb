<h2>New Topic</h2>
<div class="form">
  <%= form_for @topic, url: forum_topics_path(@forum), class: "center" do |f|  %>
    <div class="input-field">
      <%= f.label "Title" %><br>
      <%= f.text_field :title, class: "materialize-textfield" %>
    </div>
    <div class="input-field">
      <%= f.label "Description" %><br>
      <%= f.text_area :description, class: "materialize-textarea" %>
    </div>
      <%= f.hidden_field :forum_id, value: @forum.id %>
      <%= f.hidden_field :user_id, value: current_user.id %>
    <p class="submit-p">
      <%= f.submit "Create Topic", class: "waves-effect waves-light btn center" %>
    </p>
  <% end %>
</div>
